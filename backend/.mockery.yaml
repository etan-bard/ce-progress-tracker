include-auto-generated: true
filename: "mocks.go"
log-level: debug
structname: "{{.Mock}}{{.InterfaceName}}"
pkgname: "{{.SrcPackageName}}"
recursive: true
template: testify
packages:
  ce-progress-tracker/database:
    config:
      all: true
  ce-progress-tracker/services:
    config:
      all: true